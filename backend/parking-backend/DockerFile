FROM eclipse-temurin:17-jdk-alpine

# Install Maven
RUN apk add --no-cache maven

WORKDIR /app
COPY . .

# Build the application using mvn (not mvnw)
RUN mvn clean package -DskipTests

EXPOSE 8080

# Run the jar file
CMD ["sh", "-c", "java -jar target/*.jar"]